<resources>
    <!-- Base application theme. -->
    <style name="Theme.XpenseTracker" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/light_money_green_primary</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.XpenseTracker.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.XpenseTracker.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.XpenseTracker.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>